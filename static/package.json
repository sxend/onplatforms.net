{
  "name": "static.onplatforms.net",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parallel-webpack",
    "build-w": "parallel-webpack --watch",
    "publish": "VERSION=$(git rev-parse HEAD) && aws s3 sync ./dist/ s3://cdn.onplatforms.net/platform/${VERSION}/ && aws s3 sync ./dist/ s3://cdn.onplatforms.net/platform/latest/"
  },
  "author": "sxend",
  "license": "ISC",
  "dependencies": {
    "skeleton-css": "^2.0.4",
    "vue": "^2.0.7",
    "vue-router": "^2.0.3"
  },
  "devDependencies": {
    "babel-core": "^6.18.2",
    "babel-loader": "^6.2.8",
    "babel-preset-es2015": "^6.18.0",
    "css-loader": "^0.26.0",
    "html-loader": "^0.4.4",
    "node-sass": "^3.13.0",
    "parallel-webpack": "^1.5.0",
    "sass-loader": "^4.0.2",
    "style-loader": "^0.13.1",
    "webpack": "^1.13.3"
  }
}
